# Python Developer Boolean Search

(Python)
AND
(Django OR Flask OR FastAPI)
AND
(SQL)
AND
(AWS OR Azure)

## Senior Python Developer

(Python)
AND
(FastAPI OR Django)
AND
(Microservices)
AND
(Docker)
AND
(Kubernetes)
